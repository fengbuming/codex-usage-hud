# codex-usage-hud v1.2.0

Session Search, Navigation, and Event-Driven Renderer Edition.

## 新功能

- 会话管理新增持久化索引，可按范围和工作目录筛选，支持后台预热、进度展示、分页与物理清理。
- 会话搜索区分用户提问、助手回答、工具输出、改动文件和会话信息；正文连续短语优先，索引命中会明确提示可能无法被 Codex 原生查找定位。
- 搜索结果可直接跳转到 Codex Desktop 对应会话，并通过可拖动浮窗查看命中内容、调用原生查找、切换重复命中。
- 正文定位支持虚拟列表、折叠工作过程、多轮重复输出和按轮次定位。

## 性能与稳定性

- 会话索引在后台渐进构建并预热内存快照，减少首次搜索等待。
- Renderer 启动和刷新延后昂贵扫描，并进一步采用事件驱动更新。
- 修复索引达到 100% 后空白或冻结、清除后磁盘文件残留及重启后自动重建。
- 修复搜索来源标注、精确短语排序、工具/UI 快照噪声召回和多轮正文定位。
- 改善 Renderer 会话切换、新会话建立、活动任务刷新与工作气泡状态恢复。

## 安装与升级

Windows 用户下载 `codex-usage-hud-v1.2.0-windows-x64-setup.exe` 覆盖安装即可，现有配置和历史数据会保留。发布页同时提供 `.sha256` 文件用于校验安装包。
