
- 优化设置界面的价格更新预览交互：点击“拉取并预览”后保留带进度反馈的 loading 弹窗，按 requestId 关联异步结果，成功后切换到价格预览，提交失败/异常时清理对应 loading。
- 统一设置确认弹窗的横向边距：价格预览内容与操作栏对齐，通用确认操作栏改为 18px，迁移/删除等内容区域保持同一视觉基线。
- 验证：`unittest tests.test_renderer_hud` 109 项通过；修改文件语法检查和 `git diff --check` 通过。
- 重构价格检查入口（第二轮反馈）：去掉中间态“拉取并预览”确认弹窗，点击“检查价格更新”直接展示 `pricing_sync.pending_prices` 上次成功结果 + “最后获取：<时间>”，并提供“重新获取”按钮在后台触发 `fetchPricesPreview`（requestId 关联，关闭时 `discardPricingPreviewRequest`）。
- 提速价格拉取：`pricing_sync.fetch_pricing_snapshot` 改为 `ThreadPoolExecutor` 并发竞速 3 个镜像源（原串行 urlopen，超时 15s×3 ≈ 1 分钟），默认超时降到 8s；实测 0.9s 命中 gh-proxy.com 镜像。
- 差异具象化：预览按 model 聚合 `priceChanges`，变更单元格渲染 `<del>旧值</del><strong>新值</strong>`，变更行加底色与“新增 / N 项变更”徽标，removed 模型在提示条列出；“确认更新”改为 `canCommit = importPayload && (!official || changeCount > 0)`，无差异显示“已是最新”，无数据显示“暂无价格”。
- 数据闭环：`fetchPricesPreview` 现在持久化 `pricing_sync`（pending_prices/pending_changes/scope_provider/last_success_at 等）；`pricingImportCommit` 成功后清空 `pending_changes`、`unread_change_count` 并写入 `last_applied_at`，避免重复展示已应用的差异。新增 `scope_provider` 与 `last_applied_at` 到 `_normalize_pricing_sync`。
- 验证：test_renderer_hud 109 通过；test_pricing_sync 8 通过；test_pricing_runtime_commands 9 通过（新增提交清空差异用例）；test_config 29 通过；生成的 RENDERER_HUD_SCRIPT 通过 `node --check`。
- 价格表列宽自适应：`.codex-usage-hud-pricing-model-table` 去掉 `min-width: 650px` 硬下限，`grid-template-columns` 改为 `minmax(0, 1.7fr) repeat(5, minmax(0, 0.66fr))`（行是独立 grid，列必须同质 fr 才能跨行对齐，不能用 auto），滚动容器改 `overflow-x: hidden; overflow-y: auto`，单元格加 `overflow: hidden; white-space: nowrap`。
- 价格列表模型补全（第三轮反馈）：官方快照只维护 `CODEX_MODEL_IDS` 白名单（gpt-6-astra / gpt-5.6-sol / gpt-5.6-terra / gpt-5.6-luna），导致本地已配置且仍在售的 gpt-5.4、gpt-5.4-mini、gpt-5.5 不显示。已核实 OpenAI 文档页 gpt-5.4 与 gpt-5.4-mini 仍有效在线（$2.5/$0.25/$15、$0.75/$0.075/$4.5），并非下架。
- 实现：`pricing_sync.merge_pricing_rows(official_rows, local_prices, provider)` 做「官方 ∪ 本地」并按 model id 去重（大小写不敏感），本地独有行打 `officialMissing: true`；`fetch_pricing_snapshot` 新增 `extra_model_ids` 参数，把本地模型并入快照白名单，将来快照收录时能识别。两处调用点（runtime_commands.fetchPricesPreview、runtime_context 后台同步）都已接入。
- 差异计数修正：快照未收录不再算作 removed 变更，`changeCount` / `unread_change_count` 只统计 added/increased/decreased，避免「差异 N」被虚假下架项注水；本地独有行在 UI 上显示灰色「官方未收录」徽标，提示条改为「官方快照暂未收录 N 个本地模型…仍按本地价格统计」。缓存预览构造 commit payload 时过滤掉 officialMissing 行，避免把本地价回写成官方价。
- 验证（真实配置端到端，7 个模型）：gpt-5.4 / gpt-5.4-mini / gpt-5.5 正确显示并标记官方未收录，差异 0（修正前会误报 3）。test_renderer_hud 109、test_config 29、test_pricing_sync 9、test_pricing_runtime_commands 10、test_runtime_context 3 全通过；node --check 通过。
